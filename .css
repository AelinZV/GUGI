body {
    font-family: Arial, sans-serif;
    background-color: #003366; /* Синий цвет фона страницы */
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: 100vh;
    text-align: center;
}

.container {
    max-width: 800px;
    margin: 20px;
    padding: 20px;
    background-color: #fff;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    border-radius: 10px;
    text-align: center;
}

h1 {
    font-size: 30px; /* Увеличенный размер заголовка */
    color: #003366;
    margin-bottom: 10px;
}

.subtitle {
    font-size: 22px; /* Увеличенный размер подзаголовка */
    color: #555;
    margin-bottom: 20px;
    font-weight: bold;
}

.info {
    font-size: 18px; /* Увеличенный размер текста описания */
    color: #555;
    margin-bottom: 30px;
    text-align: left; /* Выравнивание по левому краю для удобства чтения */
}

.university-photo {
    width: 100%;
    max-width: 600px;
    height: auto;
    margin: 20px 0;
    border-radius: 8px;
    display: block;
    margin-left: auto;
    margin-right: auto;
}

.links {
    width: 100%;
    display: flex;
    justify-content: space-between; /* Размещаем ссылки по разным сторонам */
    padding: 0 40px; /* Отступы от краев страницы */
    position: absolute;
    bottom: 40px; /* Размещение ссылок внизу страницы */
    left: 0;
    right: 0;
}

.link-item {
    text-decoration: none;
    color: #007bff;
    font-size: 20px;
    border: 2px solid #007bff;
    padding: 10px 15px;
    border-radius: 5px;
    transition: background-color 0.3s, color 0.3s;
}

.link-item:hover {
    background-color: #007bff;
    color:
    body, .container, h1, .info {
        /* Стили остаются теми же, что и на главной странице */
    }
    
    .carousel {
        margin: 20px auto;
        max-width: 600px
    }
}